import { VintedIcon } from '@assets/vinted'

export const Header = () => {
  return <header className="w-full h-[52px] py-1 flex border-b-[1px] border-[#ededed] items-center">
    <VintedIcon className='text-[44px]' />
  </header>
}
